{
    "name": "vpndetection-fastify-integration",
    "version": "0.0.0",
    "private": true,
    "description": "Exercises the PUBLISHED vpndetection-fastify package against the staging API.",
    "type": "module",
    "license": "MIT",
    "engines": {
        "node": ">=22"
    },
    "dependencies": {
        "vpndetection-fastify": "^1.0.0",
        "fastify": "^5.6.1"
    },
    "scripts": {
        "test": "node --test test/*.test.mjs",
        "integration": "node scripts/run.mjs"
    }
}
